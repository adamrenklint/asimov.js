// parse
  // when template includes no partial
    // it should only add model as a dependency of itself
  // when template includes a partial
    // it should add model as a dependency of itself
    // it should add model as a dependency of the partial